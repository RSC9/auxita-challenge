Object.defineProperty(window, 'requestAnimationFrame', {
    writable: true,
    value: jest.spyOn().mockImplementation(cb => cb())
});

Object.defineProperty(window, 'cancelAnimationFrame', {
    writable: true,
    value: jest.fn().mockImplementation(cb => cb())
});